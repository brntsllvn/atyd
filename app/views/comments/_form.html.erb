<%= form_for(Comment.new, html: { class: "comment form-inline" }) do |f| %>
  <%= f.hidden_field :comic_id, value: comic.id %>
  <div class='form-group'>
    <div class="field">
      <%= f.text_field :body %>
    </div>
  </div>
    <div class='form-group'>
    <div class="actions">
      <%= f.submit "comment", class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
